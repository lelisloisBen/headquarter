import React from 'react';
import styles from './Messages.module.css';

const Messages = () => {
    return (
        <section className={styles.section}>
            <h1>Messages from Datavault Website Contact</h1>
        </section>
    );
};

export default Messages;